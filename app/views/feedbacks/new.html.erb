<%= render 'shared/errors', resource: @feedback %>

<div class="form">
  <%= form_with model: @feedback, local: true do |form| %>
    <p class="form__field">
      <%= form.label :body, t('feedback')  %>
      <%= form.text_area :body, id: :feedback_body %>
    </p>

    <p class="form__action"> <%= form.submit t('.submit') %> </p>
  <% end %>
</div>
